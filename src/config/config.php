<?php

return array(
	// List of questions to appear in your form
	// The keys are used as the questions
	// The values are used as answers
	'questions' => array(
		'What are we doing?' => ['Meta']
	),

	// Whether the checking of the answers is case-sensitive
	// E.g., 'What are we doing?' => ['Meta']
	// Anything other than `*M*eta` would be incorrect
	'case_sensitive' => true
);